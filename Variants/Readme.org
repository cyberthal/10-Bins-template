The =Variants= directory is for documentation only.  It isn't actually present in real 10 Bins instances.  

Instead it documents minor variations in 10 Bins, which depend on the context in which it is deployed.
